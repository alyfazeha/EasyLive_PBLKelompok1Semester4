# Resolve Git Merge Conflicts After Stash Pop + Pull

## Steps:
- [x] 1. Create this TODO.md
- [x] 2. Edit easylive_app/lib/views/splash/splash_view.dart (merged to use logo-easylive.png + width:200 + fit)
- [x] 3. Edit easylive_app/pubspec.yaml (specific assets list + easylive.png)
- [ ] 4. Run git commands: add both files, commit, checkout rafi
- [ ] 5. Run flutter pub get
- [x] 6. Update TODO.md ✅ Conflicts resolved!

**Merge conflicts resolved! Files cleaned up with merged changes (official logo + your sizing, full specific assets).**

**Next (run these in terminal):**
```
git add easylive_app/lib/views/splash/splash_view.dart easylive_app/pubspec.yaml
git commit -m "resolve merge conflict after stash pop"
git checkout rafi
flutter pub get
```

